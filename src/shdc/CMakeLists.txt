fips_begin_app(sokol-shdc cmdline)
    fips_src(.)
    fips_deps(fmt getopt pystring glslang SPIRV-Cross)
fips_end_app()
if (FIPS_GCC)
    target_compile_options(sokol-shdc PRIVATE -Wno-unused-result)    
endif()
